//placeholder stuff
function bolt_clearance(hw) =
    hw == "M4" ? 4.5 :
    hw == "M5" ? 5.5 :
    hw == "M6" ? 6.6 : undef;

function nut_flat(hw) =
    hw == "M4" ? 7.0 :
    hw == "M5" ? 8.0 :
    hw == "M6" ? 10.0 : undef;
